let numeros = [10,5,8,12]

let soma = 0

numeros.forEach(numeros => {
    soma += numeros
    media = soma / 4
})

for(let i = 0; i <= 4; i++){
    if(numeros[i] > 8.75){
        console.log(numeros[i])
    }
}